import './ContactPromise.css'

const ContactPromise = () => {
  return (
    <div className="contact-promise">
        <h2>"Reply within one business day, every time."</h2>
    </div>
  );
};

export default ContactPromise;